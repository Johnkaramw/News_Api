import 'package:flutter/material.dart';

class PhonsPage extends StatelessWidget {
  const PhonsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title:const Text('phons'),),);
  }
}