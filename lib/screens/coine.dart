import 'package:flutter/material.dart';

class CoinePage extends StatelessWidget {
  const CoinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title:const Text('coine'),
      ),
    );
  }
}